// 2026-06-29: ABANDONED — never deployed.
//
// Justin opted to stick with the MT iframe embed on /trial/[studio] rather
// than build out a native form. This file is left as a stub so the directory
// doesn't disappear from git; nothing imports it and it's never called.
//
// If we ever revisit native form → MT API, see git history for the full
// implementation (commit comment "trial: native BBB form → MT API (no iframe)"
// — never shipped to prod).
export {};
